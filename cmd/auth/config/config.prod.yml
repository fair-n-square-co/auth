port: 8081
logger:
  level: 0
  format: json
db:
  connString: ""
  maxConns: 20
  minConns: 5
  maxConnLifetime: 1h
  maxConnIdleTime: 30m
  healthCheckPeriod: 1m
workos:
  # Env-specific values come from AUTH_WORKOS_* env vars, not the YAML.
  issuer: ""
  clientID: ""
